code to implement content in Mathematical Foundation Of Reinforcement Learning

The `tabular` folder contains code for chapter `1-7`, where 
state, action and policy are represented in tabular form. 

The `function` folder contains code for chapter `8-10`, where
state, action and policy are represented in function form.
